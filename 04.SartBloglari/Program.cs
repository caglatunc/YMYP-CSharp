Console.WriteLine("Hello, World!");

//if bloğu
//else if bloğu
//switch bloğu

int a = 1;
int b = 1;

//syntax => bir kodun nasıl yazılması gerektiğidir
//  && => ve operatörü iki şartında sağlanması
// || => veya operatörü. Dik çizgileri Altgr+tire işareti ile yapılıyor.
//if (a < 3 && b==1 || 3==3 || a>=1 || a<=2 | a!=1)
//{ //scope
//    console.Writeline("a değeri 3'den küçük bir değer!");
//}
//else 
//{
//    console.Writeline("a değeri 3 veya daha büyük bir değer!");
//}

// ctrl + k + c => açıklama satırı yapar
// ctrl + k + u => açıklama satırını kaldırır

if (a >0)
{
    if(a == 1)
    {
    //şunu yap
    }
    if(a==2)
    {
        //şunu yap
    }
}else if  (b == 1)
{ 

}
else
{
    
}

switch(a)
{
    case "Ahmet":
        //işlem
        break;
    case "Seval":
        //işlem
        break;
    default:
        //işlem
}

int kdv = 18;

switch(kdv)
{
    case 1:
        //işlem
        break;
    case 10:
        //işlem
        break;
    case 18:
        //işlem
        break;
}